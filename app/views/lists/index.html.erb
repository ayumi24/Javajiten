<h3 class="heading">これまでの記録</h3>
  <table class="table">
    <thead>
      <th>タイトル</th>
    </thead>
    <tbody>
      <% @lists.each do |list| %>
        <tr>
          <td><%= link_to list.title, list_path(list.id) %></td>
        </tr>
    </tbody>
      <% end %>
  </table>
